apiVersion: apps/v1
kind: Deployment
metadata:
  name: test-again
  namespace: test
spec:
  replicas: 350
  selector:
    matchLabels:
      name: test-again
  template:
    metadata:
      labels:
        name: test-again
    spec:
      containers: invalid string
