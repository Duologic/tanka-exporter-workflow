apiVersion: apps/v1
kind: Deployment
metadata:
  name: test-test
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      name: test-test
  template:
    metadata:
      labels:
        name: test-test
    spec:
      containers: []
