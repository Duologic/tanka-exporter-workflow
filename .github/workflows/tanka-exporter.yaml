# Generated with `make gen`
jobs:
  show:
    runs-on: "ubuntu-latest"
    steps:
      - name: "checkout"
        uses: "actions/checkout@v4"
      - name: "install tanka"
        uses: "./.github/actions/install-tanka"
      - run: "tk export -r ../_out ."
        working-directory: "jsonnet"
      - run: "ls _out"
"on": "push"