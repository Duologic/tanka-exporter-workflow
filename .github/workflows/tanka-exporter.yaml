# Generated with `make gen`
jobs:
  show:
    runs-on: "ubuntu-latest"
    steps:
      - name: "checkout"
        uses: "actions/checkout@v4"
      - name: "install tanka"
        uses: "./.github/actions/install-tanka"
      - run: "tk export --recursive --format '{{ if env.metadata.labels.cluster_name }}{{ env.metadata.labels.cluster_name }}/{{ end }}{{ if .metadata.namespace }}{{.metadata.namespace}}{{ else }}_cluster{{ end }}/{{.kind}}-{{.metadata.name}}' ../_out environments/ "
        working-directory: "jsonnet"
      - run: "ls _out"
"on": "push"